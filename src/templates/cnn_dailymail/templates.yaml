dataset: cnn_dailymail
splits:
  train: train
  validation: validation
  test: test
templates:
  T5:
    - name: cnn_dailymail-prompt-t5
      input: 'summarize: {{article}}'
      target: '{{highlights}}'
      task: summarization
      metadata:
        languages:
          - en
        metrics:
          - rouge
